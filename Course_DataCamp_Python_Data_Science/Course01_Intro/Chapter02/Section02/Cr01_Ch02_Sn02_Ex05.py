# Use info() to inspect mpr
print(mpr.info())

# The following code contains one or more errors
# Correct the mistakes in the code so that it runs without errors

# Select column "Dog Name" from mpr
name = mpr['Dog Name']   #added brackets and quote marks - replaced dot notation

# Select column "Missing?" from mpr
is_missing = mpr['Missing?']   #added brackets and quote marks - replaced dot notation

# Display the columns
print(name)
print(is_missing)